const formatAddress = require("./formatAddress");
const formatMessage = require("./formatMessage");
const resolveIPFSHash = require("./resolveIPFSHash");

module.exports = { resolveIPFSHash, formatAddress, formatMessage };
